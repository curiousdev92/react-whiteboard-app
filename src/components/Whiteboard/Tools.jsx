import Button from "../Button";

const Tools = () => {
  return (
    <div className="whiteboard__tools">
      <Button text="pen" />
      <Button text="eraser" />
      <Button text="select" />
      <Button text="shapes" />
      <Button text="arrow" />
      <Button text="text" />
      <Button text="delete" />
    </div>
  );
};

export default Tools;
